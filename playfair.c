#include<stdio.h>
#include<string.h>
#include<stdlib.h>

void playfair(char str[],charkeystr[])
{
char matrix[5][5];
char key=strlen(keystr);
char plaintext=strlen(str);
  void touppercase(charencrypt[],intps)
{
	for(int i=0;i<plaintext;i++)
}

}
